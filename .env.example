# Copy to .env and fill in. Never commit the real .env file.

POSTGRES_USER=taxapp
POSTGRES_PASSWORD=change-me
POSTGRES_DB=taxapp

# Hosted LLM API (classification + citation-grounded generation) — see
# docs/System_Design_and_Requirements.md §6 for the model choice rationale.
LLM_API_KEY=
